# Saved Model Directory

This directory is created and updated automatically when you run `train.py`.

## Contents after training

- `model.joblib`: Trained vectorizer + classifier pipeline
- `meta.json`: Labels, thresholds, and evaluation summary

`model_weights.pt` is not used by this lightweight local setup.
